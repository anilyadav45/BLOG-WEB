<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
</head>

<body>
    <%- include('layout.ejs') %>

        <div class="container main">
            <h1 class="mt-2 page-title">Capture Moments. Express Freely. Inspire Others 💡</h1>
            <a href="/blogs/new" class="btn btn-success mb-3">📝 Create a New Post</a>

            <% if (blogs && blogs.length> 0) { %>
                <% blogs.forEach(blog=> { %>
                    <div class="post-card my-4">
                        <div class="post-header">
                            <div class="user-info">
                                <img src="https://i.pravatar.cc/40" alt="User Avatar" class="avatar">
                                <span class="username">Username</span>
                            </div>
                        </div>

                        <div class="post-body">
                            <h3 class="post-title">
                                <%= blog.title %>
                            </h3>
                            <img src="https://thumbs.dreamstime.com/b/morning-meditation-29552916.jpg?w=768"
                                alt="<%= blog.title %>" class="post-image">
                            <p class="post-text">
                                <%= blog.content %>
                            </p>
                        </div>

                         <div class="post-actions">
                            <a href="/blogs/<%= blog._id %>" class="btn btn-read-more">Read More</a>
                            <a href="/blogs/<%= blog._id %>/edit" class="btn btn-edit">Edit</a>
                            <form action="/blogs/<%= blog._id %>?_method=DELETE" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-delete">Delete</button>
                            </form>
                        </div>

                        <div class="engagement-stats">
                            <span><i class="fas fa-heart"></i> 25</span> <span><i class="fas fa-comment"></i> 12</span>
                            <span><i class="fas fa-share"></i></span>
                        </div>

                        <div class="comment-section">
                            <form action="/comment" method="POST" class="comment-form">
                                <img src="https://i.pravatar.cc/30" alt="Your Avatar" class="avatar-comment"> <input
                                    type="text" name="comment" class="form-control comment-input"
                                    placeholder="What’s on your mind? Share your thoughts...">
                                <button type="submit" class="btn btn-comment">Comment</button>
                            </form>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <p class="text-center text-white">No posts yet. Be the first to share something!</p>
                            <% } %>
        </div>

        <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>

</html>